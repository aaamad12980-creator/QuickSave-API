'use strict';

/**
 * Models directory placeholder.
 *
 * QuickSave does not currently use a database. This file exists to keep
 * the Clean Architecture folder structure intact and ready for future
 * scaling (e.g. adding Mongoose schemas, Sequelize models, or Prisma
 * models here once persistence is introduced).
 */

module.exports = {};
